{
  "name": "ipfs-message-port-server-type-error",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prebuild": "rimraf lib",
    "build": "tsc"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "ipfs": "^0.62.2",
    "ipfs-core": "^0.14.2",
    "ipfs-message-port-server": "^0.11.2"
  }
}
